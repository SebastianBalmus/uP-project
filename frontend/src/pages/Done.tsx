import React from 'react';
import Layout from '../components/Layout/Layout';
import SessionFinished from '../components/SessionFinished/SessionFinished';

const Done: React.FC = () => (
  <Layout>
    <SessionFinished />
  </Layout>
);

export default Done;
